class <%= @infos[:specs]['class_name'].capitalize %>
  def initialize(args)
  # args can receive an object from the source, see their documentation for more informations
  # If you need to communicate with a client, you have to retrieve it like this :
    # @twitter_client = Jarvis::ThirdParty::Registered.client(:twitter)
    # register your client in this method, and use it everywhere else.
  # Clients name should be normalized. See client documentation, or Jarvis to know how they are normalized.
  end
end