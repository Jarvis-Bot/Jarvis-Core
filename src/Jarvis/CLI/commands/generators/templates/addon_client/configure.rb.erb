# This file will be indexed by Jarvis automatically when the user type
# ./jarvis configure
# You can ask to the user some needed informations, for example, API keys or
# username and save them into a config.yml file, at the root of your addon.
class Configure
  def initialize
    # Will be called by Jarvis
  end
end
